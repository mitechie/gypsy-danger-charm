name: gypsy-danger
summary: Parse server logs and dump into mysql for reporting
maintainer: Richard Harding <rick.harding@canonical.com>
description: |
  Parse Apache logs into the DB.
subordinate: false
series: [xenial]
provides:
  grafana-source:
    interface: "grafana-source"
requires:
  db:
    interface: mysql
